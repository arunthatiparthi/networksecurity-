import socket
import base64
from _thread import*
ServerSocket =Socket.socket()
host ='127.0.0.1'
port='8080'
print('waiting connection')
try:
    ServerSocket.bind((host,port))
except socket.error as arun:
    print(str(arun))
Response=client

    
    